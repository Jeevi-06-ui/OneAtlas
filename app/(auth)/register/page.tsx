import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
  title: "Create account",
  description: "Register for OneAtlas Runtime Builder",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
